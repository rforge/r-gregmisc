#!/bin/sh 

if [ $# != 1 ]; then
	echo "Usage: $0 <data file>"
	exit 1
fi

lsfenvfile=$1; shift

R --vanilla <<EOF

newenv <- environment()
load("${lsfenvfile}", envir=newenv)
lsf.ret <- eval(lsf.call, env=newenv)
save(lsf.ret, file = "${lsfenvfile}")

EOF
