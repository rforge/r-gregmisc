
export(
       Args,
       aggregate.table,
       combine,
       elem,
       env,
       interleave,
       is.what,
       keep,
       ll,
       matchcols,
       nobs,
       read.xls,
       rename.vars,
       remove.vars,
       reorder.factor,
       trim
)

importFrom(gtools, odd, invalid, mixedsort)

S3method(nobs,data.frame)
S3method(nobs,default)
S3method(nobs,lm)
S3method(reorder,factor)

S3method(ll,default)
S3method(ll,data.frame)
S3method(ll,list)
